{
  "name": "dataverse",
  "version": "1.15.0",
  "description": "Microsoft Dataverse plugin for coding agents, covering record CRUD, bulk data operations, advanced queries, schema and metadata, administration, security, environment management, and solution lifecycle through MCP, CLI, and SDK workflows.",
  "settings": [
    {
      "name": "Dataverse environment URL",
      "description": "The confirmed Dataverse environment used by this workspace.",
      "envVar": "DATAVERSE_URL",
      "sensitive": false
    }
  ],
  "mcpServers": {
    "dataverse": {
      "command": "npx",
      "args": [
        "-y",
        "@microsoft/dataverse@1.0.77",
        "mcp",
        "${DATAVERSE_URL}"
      ],
      "env": {
        "DATAVERSE_OPERATION_CONTEXT": "app=dataverse-skills/1.15.0;skill=mcp-direct;agent=gemini-cli"
      }
    }
  }
}